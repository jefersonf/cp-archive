#include <bits/stdc++.h>
using namespace std;

void opt() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
}

void solve(int tc) { 
    //cout << "test case: " << tc << endl;
}

int main() {
    opt();

    freopen("tc.in", "r", stdin);
    //freopen("tc.out", "w", stdout);

    int tc=1;
    //cin >> tc;
    while (tc--)
    solve(tc);
    return 0;
}

